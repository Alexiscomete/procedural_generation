package procedural_generation.noise.nodes;

import procedural_generation.noise.Node;

public class ChangeSeedNode implements Node {
    @Override
    public double getValue(double x, double y) {
        return 0;
    }
}
